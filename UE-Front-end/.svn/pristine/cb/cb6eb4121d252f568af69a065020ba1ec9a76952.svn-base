import React from "react";

interface CardInrProps {
    title?: string,
    className?: string,
    bodyStyle?: string,
    headStyle?: string,
    containerStyle?: string,
    children?: any
}

const CardInr: React.FC<CardInrProps> = (props) => {
    const { title, className, bodyStyle, headStyle, containerStyle, children } = props

    return (
        <div>
            {children}
        </div>
    )
}

export const Card = CardInr